source "https://rubygems.org"

# The specs run the space rules against an in-memory harness, so they
# need nothing but rspec - no Redis, no EventMachine, no game server.
group :test do
  gem "rspec"
end
